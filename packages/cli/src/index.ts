/**
 * @durion/cli — public API.
 *
 * Projects import `defineConfig` from this package in their `durion.config.ts`.
 */
export { defineConfig } from './config';
export type { DurionConfig } from './config';
