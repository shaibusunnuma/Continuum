/**
 * Shared Temporal settings for this example (worker + client stay aligned).
 * Not required by the SDK — just avoids duplicating the queue name in two entry files.
 */
export const COMPOSABILITY_TASK_QUEUE = 'ai-runtime-composability';
